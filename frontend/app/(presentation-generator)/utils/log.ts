 // Add logging function
export const logOperation = (message: string) => {
  console.log(`[DECK-GENIE] ${message}`);
};

  // Add clear logs function
export const clearLogs = () => {
  console.clear();
};